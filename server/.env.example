DATABASE_URL=postgresql://user:password@host/dbname?sslmode=require
JWT_SECRET=change-me
PORT=4000
CLIENT_ORIGIN=http://localhost:5173
NODE_ENV=development
SEED_ADMIN_PHONE=6302350963
SEED_ADMIN_NAME=Platform Admin

# Optional: set this to send real OTP SMS via Fast2SMS (free credits on
# signup, no DLT registration needed for the Quick SMS route it uses).
# Leave blank to keep using the console-log fallback for testing.
# Get one at https://www.fast2sms.com -> Dashboard -> Dev API
FAST2SMS_API_KEY=
